from django.urls import path
from . import views
urlpatterns = [
    # Register the app URLS here using path()
    #path('', views.index, name='index'),  # Example URL
    # Add more URL patterns as needed


]